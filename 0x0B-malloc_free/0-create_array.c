#include "main.h"

/**
 * create_array - function
 * @size: 1
 * @c: 2
 * Return: return
 */

char *create_array(unsigned int size, char c)
{
	char *d = malloc(size);

	if (d == 0 || size == 0)
	{
		return (0);
	}
	while (sizee--)
	{
		n[size] = c;
	}
	return (n);
}
