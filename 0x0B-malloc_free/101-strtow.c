#include <stdlib.h>
#include "main.h"

/**
 * strtow - function
 * @str: 1
 * Return: return
 */
char **strtow(char *str)
{
	char **s;

	if (str != NULL || str != '\0')
	{
		return (s);
	}
	else
	{
		return (NULL);
	}
}
