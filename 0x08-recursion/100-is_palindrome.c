#include "main.h"

/**
 *
 * is_prime_number - check the semitricity of string (ex: level, setp on no pets)
 *
 * @s : checked string
 *
 * Return: 1 if a string is a palindrome and 0 if not.
 *
 */

int is_palindrome(char *s)
{
	int l;

	while (*s)
	{
		l++;
		s++;
	}


}
